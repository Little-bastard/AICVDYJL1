# 2024-09-24
1. 显微镜部分使用多线程处理图像和ui

# 2024-09-23
1. 给大模型提供三个接口：输入接口——输入参数配置；查询接口——查询结果及中间状态；控制启停接口——控制启动和停止。
2. 讨论上面的查询接口需要查询什么；
3. 安装一个实验平台全景的摄像头，大模型能够查询摄像头视频。

# 2024-09-21
1. 选择参数文件后，重置按钮“应用”状态；
2. 写入温度程序后，及时更新温度曲线图像的横纵轴刻度范围；
3. 流量计数值可能出现小于0的情况，此时显示值却很大，溢出了
4. 讨论确定整个实验结束动作是什么。

# 2024-09-20
使用pyinstaller打包项目为exe方法如下：
1. 在program目录下运行命令：\
>pyi-makespec -i "icon/aicvd.ico" -F --add-binary "MicroscopeDev/toupcam.dll;."  --exclude-module PyQt6 .\aicvd.py \
pyinstaller aicvd.spec
2. 生成的aicvd.exe在dist目录下，将aicvd.exe拷贝到program目标下，即可双击运行；再将program下的aicvd.exe创建快捷方式到桌面，即可在桌面双击运行

# 2024-08-16
1. 显微镜快照和视频时，卡顿，拟使用多线程解决；
2. 在表格里writeProgram时使用槽函数，将逻辑与UI分开；
3. 表格下方添加删除行的按钮；

# 2024-08-07
1. 计划给MFC写个多线程，用于轮询读取数据；
2. 计划给MFC预留可扩展设备号，用列表或者对象来处理；

# 2024-07-30
1. 保存显微镜视频，同时保存温度曲线

# 2024-07-24
1. 撰写功能需求
2. 软件开发。温度控制软件:展示设置好的程序段，preview功能；
3. 温度曲线刻度范围按照与当前程序段一致;
4. 写入程序段时，添加正在写入中的提示框；
